import React from 'react';
import { Container, Typography, Box } from '@mui/material';

const Home = () => {
  return (
    <Container>
      <Box sx={{ mt: 4 }}>
        <Typography variant="h2">Welcome to the Food Ordering App</Typography>
        {/* Add more components or content as needed */}
      </Box>
    </Container>
  );
};

export default Home;
